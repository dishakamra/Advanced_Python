'''
Exercise 1 — Write & call a function

Task: Create shout(text) that returns the text upper-cased with an exclamation.
'''


'''
Exercise 2 — Default parameter & keyword-only

Task: Build format_name(first, last,*, uppercase=False) that returns "last, first". If uppercase=True, return it upper-cased.
'''

'''
Exercise 3 — *args and **kwargs

Task: Write summarize(*nums, **opts) that prints the count and optionally the label (a keyword) before the sum.
'''

'''
Exercise 4 — Build a tiny package

Goal: Use a package with __init__.py to expose a clean API.

calc_pkg/
  __init__.py
  add.py
  mul.py
'''